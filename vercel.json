{
  "version": 2,
  "builds": [
    {
      "src": "packages/app-shell/package.json",
      "use": "@vercel/next",
      "config": {
        "distDir": ".next"
      }
    }
  ],
  "buildCommand": "yarn install && yarn build",
  "outputDirectory": "packages/app-shell/.next"
}
